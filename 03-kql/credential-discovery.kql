```kql
SecurityEvent
| where TimeGenerated > ago(2h)
| where EventID == 4688
| where CommandLine has_any ("cmdkey", "net user", "C:\\Users", "dir")
| project TimeGenerated, Computer, Account, NewProcessName, CommandLine, ParentProcessName
| order by TimeGenerated desc
```
